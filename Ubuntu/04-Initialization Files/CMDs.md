# CMDs

```bash

set --> view all environment variables which has value

man bash , then search for shell variables will give me all environment variables

printenv --> to search for specific variable

alias newname=oldname --> to make a shortname of any long cmd or most used
# it also temp so if i want it permenant --> bashrc
# if i make a new name like existed cmd and i want to use original one or existed
# one --> write '/' before using it */

unalias newname --> return cmd to original name
```
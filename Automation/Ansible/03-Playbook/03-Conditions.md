# 03-Conditions

```yaml
# at the same level of name of module 

when: # any logic we need especially we use the facts that depends on system 

# we can use and , or , not , == , >= , <= 
# if condition == false -> the task will be skipped 
```